<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.pe.ryudaewan.brano.user.dao.UserDao">
    <resultMap id="userResultMap" type="kr.pe.ryudaewan.brano.user.service.UserVo"
               extends="kr.pe.ryudaewan.brano.base.dao.CommonDao.commonResultMap">
        <id column="uid" property="uid"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
    </resultMap>

    <select id="selectUserByUid" resultMap="userResultMap" parameterType="java.lang.Long">
        /* UserDao.selectUserByUid */
        select u.uid
        , u.name
        , u.email
        , u.created_at
        , u.updated_at
        , u.deleted_at
        from users u
        where u.uid = #{uid}
        and u.deleted_at is null
    </select>

    <select id="selectUsers" resultMap="userResultMap">
        /* UserDao.selectUsers */
        select u.uid
        , u.name
        , u.email
        , u.created_at
        , u.updated_at
        , u.deleted_at
        from users u
        where u.deleted_at is null
    </select>

    <insert id="insertUser" parameterType="kr.pe.ryudaewan.brano.user.service.UserVo">
        <selectKey keyProperty="uid" resultType="long" order="BEFORE">
            /* UserDao.insertUser */
            select coalesce(max(u.uid), -1) + 1 from users u
        </selectKey>
        /* UserDao.insertUser */
        insert into users(uid, name, email, created_at)
        values (#{uid}, #{name}, #{email}, #{createdAt} )
    </insert>

    <update id="updateUser" parameterType="kr.pe.ryudaewan.brano.user.service.UserVo">
        /* UserDao.updateUser */
        update users u set u.name = #{name}
        , u.email = #{email}
        , u.updated_at = #{updatedAt}
        where u.uid = #{uid}
        and u.deleted_at is null
    </update>

    <update id="deleteUser" parameterType="java.lang.Long">
        /* UserDao.deleteUser */
        update users u set u.deleted_at = now()
        where u.uid = #{uid}
        and u.deleted_at is null
    </update>
</mapper>